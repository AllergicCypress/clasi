click>=8.0
pyyaml>=6.0
rich>=13.0
mutagen>=1.45    # tags de audio (ID3, Vorbis, MP4); fallback a ffprobe si no está
Pillow>=10.0     # OCR de imágenes y PDFs escaneados; EXIF
pytesseract>=0.3 # wrapper Python para Tesseract OCR
